{
  "_comment": "WebDAV 접속 정보(URL/username/password/baseDir)는 ~/.wormhole/.env 의 플랫 변수 WEBDAV_URL/USER/PASS/BASEDIR 로 지정한다. 이 파일의 remote 섹션은 옵션 base 이며 .env 변수가 그 위를 override 한다. passphrase 원문은 절대 이 파일에 넣지 않는다(~/.wormhole/.env 또는 0600 파일 / keychain 사용).",
  "stateDir": "~/.wormhole",
  "crypto": {
    "_comment": "passphraseEnv: passphrase 를 읽을 환경변수 이름. passphraseFile/derivedKeyPath 는 빈 문자열이면 stateDir 기본값을 쓴다(passphraseFile=<stateDir>/passphrase, derivedKeyPath=<stateDir>/age-key.txt). kdfN 이 클수록 강하다(메모리 사용량 증가). N=65536=2^16 ≈ 64MB.",
    "passphraseEnv": "WORMHOLE_PASSPHRASE",
    "passphraseFile": "",
    "derivedKeyPath": "",
    "kdfN": 65536,
    "kdfR": 8,
    "kdfP": 1
  },
  "targets": {
    "_comment": "include/exclude 는 동기화 업로드 대상 glob 목록이다.",
    "include": [
      ".claude/CLAUDE.md",
      ".claude/settings.json",
      ".claude/skills/**",
      ".claude/agents/**",
      ".claude/commands/**",
      ".claude/.mcp.json",
      ".claude/hooks/**",
      ".claude/statusline/**",
      ".claude/hud/**"
    ],
    "exclude": [
      ".claude/.credentials.json",
      ".claude/settings.local.json",
      "**/*.token",
      "**/*.key",
      ".claude/projects/**",
      ".claude/todos/**",
      ".claude/statsig/**",
      ".claude/history/**",
      "**/*.log",
      "**/cache/**"
    ]
  },
  "settingsLocalKeys": [
    "mcpServers.*.command",
    "mcpServers.*.args",
    "mcpServers.*.cwd",
    "mcpServers.*.env",
    "permissions.*",
    "hooks",
    "statusLine.command"
  ],
  "selfMcpServerNames": ["wormhole"],
  "conflictPolicy": "preserve-both",
  "lock": {
    "ttlMs": 30000,
    "acquireRetries": 3,
    "acquireRetryDelayMs": 1000
  }
}
